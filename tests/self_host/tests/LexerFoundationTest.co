import self_host.checks.lexer::LexerFoundationCheck;

import testing::Assert;
import testing::TestFailure;

class {
  static func Run() throws IoError, StateError, TestFailure {
    Assert.Equal(0, LexerFoundationCheck.Run(),
                 "lexer foundation check status");
  }
}
