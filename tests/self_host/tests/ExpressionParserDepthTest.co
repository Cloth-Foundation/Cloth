import self_host.checks.parser::ExpressionParserDepthCheck;

import testing::Assert;
import testing::TestFailure;

class {
  static func Run() throws StateError, TestFailure {
    Assert.Equal(0, ExpressionParserDepthCheck.Run(),
                 "expression parser depth check status");
  }
}
