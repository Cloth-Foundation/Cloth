import self_host.checks.parser::DeclarationSubstrateGcCheck;

import testing::Assert;
import testing::TestFailure;

class {
  static func Run() throws StateError, TestFailure {
    Assert.Equal(0, DeclarationSubstrateGcCheck.Run(),
                 "declaration substrate GC check status");
  }
}
