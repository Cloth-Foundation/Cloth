import self_host.checks.syntax::SyntaxArenaCheck;

import testing::Assert;
import testing::TestFailure;

class {
  static func Run() throws StateError, TestFailure {
    Assert.Equal(0, SyntaxArenaCheck.Run(), "syntax arena check status");
  }
}
