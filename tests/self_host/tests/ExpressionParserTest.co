import self_host.checks.parser::ExpressionParserCheck;

import testing::Assert;
import testing::TestFailure;

class {
  static func Run() throws IoError, StateError, TestFailure {
    Assert.Equal(0, ExpressionParserCheck.Run(),
                 "expression parser check status");
  }
}
