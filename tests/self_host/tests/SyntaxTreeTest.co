import self_host.checks.syntax::SyntaxTreeCheck;

import testing::Assert;
import testing::TestFailure;

class {
  static func Run() throws StateError, TestFailure {
    Assert.Equal(0, SyntaxTreeCheck.Run(), "syntax tree check status");
  }
}
