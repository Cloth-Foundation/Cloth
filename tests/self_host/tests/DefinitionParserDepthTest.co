import self_host.checks.parser::DefinitionParserDepthCheck;

import testing::Assert;
import testing::TestFailure;

class {
  static func Run() throws StateError, TestFailure {
    Assert.Equal(0, DefinitionParserDepthCheck.Run(),
                 "definition parser depth check status");
  }
}
