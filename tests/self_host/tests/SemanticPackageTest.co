import self_host.checks.semantic::SemanticPackageCheck;

import testing::Assert;
import testing::TestFailure;

class {
  static func Run() throws IoError, StateError, TestFailure {
    Assert.Equal(0, SemanticPackageCheck.Run(),
                 "semantic package check status");
  }
}
