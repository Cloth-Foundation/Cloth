import self_host.checks.frontend::FrontendCoordinatorCheck;

import testing::Assert;
import testing::TestFailure;

class {
  static func Run() throws StateError, TestFailure {
    Assert.Equal(0, FrontendCoordinatorCheck.Run(),
                 "frontend coordinator check status");
  }
}
