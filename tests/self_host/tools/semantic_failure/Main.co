import self_host.checks.semantic::SemanticPackageFailure;

static func Main(string[] args): int32 throws IoError, StateError {
  if (args::length != 1) { return 2; }
  return SemanticPackageFailure.Run(args[0]);
}
