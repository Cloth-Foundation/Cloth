import self_host.checks.parser::DeclarationSubstrateFailure;

static func Main(string[] args): int32 throws StateError {
  if (args::length != 1) { return 2; }
  return DeclarationSubstrateFailure.Run(args[0]);
}
