import self_host.parity::DeclarationRecordWriter;

static func Main(string[] args): int32 throws IoError, StateError {
  if (args::length != 2) { return 2; }
  return DeclarationRecordWriter.Run(args[0], args[1]);
}
