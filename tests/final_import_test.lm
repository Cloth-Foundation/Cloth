// Comprehensive import test
import std::io::{printf, print, println};
import std::strings::{length, substring, concat};
import std::math::{add, subtract, multiply};
import std::nonexistent::{someFunction}; // This should fail gracefully

// Test that valid imports work even with failed imports
var message -> string = "Hello, World!";
var num1 -> i32 = 10;
var num2 -> i32 = 5;

print(message);
println("Length: " + length(message));
printf("Math: %d + %d = %d, %d * %d = %d", [num1, num2, add(num1, num2), num1, num2, multiply(num1, num2)]); 