#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int add(int a, int b);
int multiply(int x, int y);
void main();


int add(int a, int b) {
    return (a + b);
    return 0;
}

int multiply(int x, int y) {
    return (x * y);
    return 0;
}

void main() {
    int result = add(5, 3);
    int product = multiply(result, 2);
    if ((result > 5)) {
    int temp = 10;
    } else {
    int temp = 20;
    }
    int i = 0;
    while ((i < 10)) {
    (i = (i + 1));
    }
}

