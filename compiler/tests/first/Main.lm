mod first

pub func main(): i32 {
    var count = 0

    do {
        printf("Count is: %s\n", count)
        count++
    } while count < 10

    printf("Loop finished. Final count: %s\n", count)

    while count < 20 {
        printf("Count is: %s\n", count)
        count++
    }

    ret 0
}