mod first

import first.Human::{ Human, Color }

pub func main(): i32 {
    var john: Human = new Human("John", 20, Color.BLUE)
    println(john.getName())
    println(john.getAge())
    println(john.getFavoriteColor().toString())

    var mike: Human = new Human("Mike", 25, Color.GREEN)
    println(mike.getName())
    println(mike.getAge())
    println(mike.getFavoriteColor().toString())

    ret 0
}