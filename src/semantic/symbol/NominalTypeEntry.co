import semantic.identity::NominalIdentity;
import semantic.model::TypeId;

class {
  final NominalIdentity identity;
  final TypeId type;

  _NominalTypeEntry(NominalIdentity identity, TypeId type) {
    self.identity = identity;
    self.type = type;
  }

  static func Build(NominalIdentity identity,
                    TypeId type): NominalTypeEntry {
    return NominalTypeEntry(identity, type);
  }

  func GetIdentity(): NominalIdentity { return self.identity; }
  func GetType(): TypeId { return self.type; }
}
